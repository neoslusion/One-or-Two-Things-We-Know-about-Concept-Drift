\chapter{Thực nghiệm và đánh giá}
\label{chap:experiments}

\section{Tổng quan thực nghiệm}
\label{sec:overview}

\subsection{Mục tiêu và Cấu trúc chương}
Chương này trình bày kết quả thực nghiệm để đánh giá hệ thống đề xuất. Đánh giá gồm ba phần, tương ứng với ba module của hệ thống:
\begin{enumerate}
	\item \textbf{Đánh giá khả năng phát hiện drift:} so sánh ShapeDD và biến thể cải tiến với các phương pháp nền tảng trên nhiều tập dữ liệu, tập trung vào sudden drift là kịch bản ShapeDD được thiết kế để xử lý.
	\item \textbf{Đánh giá khả năng phân loại drift của SE-CDT:} kiểm tra độ chính xác khi nhận diện loại drift (sudden, gradual, incremental, recurrent, blip) dựa trên tín hiệu drift magnitude.
	\item \textbf{Đánh giá khả năng thích ứng:} đo hiệu quả của các chiến lược cập nhật mô hình tự động qua chỉ số Prequential Accuracy.
\end{enumerate}

\subsection{Môi trường và Cấu hình}
\label{sec:dataset-config}
Các tham số môi trường và thuật toán được mô tả gắn với từng kịch bản (Detection, Classification, Adaptation) thay vì trình bày chung, để dễ tái lập từng phần.

\subsection{Tập dữ liệu thực nghiệm}

Hệ thống được đánh giá trên bộ \textbf{14 tập dữ liệu}, gồm các tập tổng hợp (synthetic), một tập gradual drift được thiết kế thủ công, một tập recurrent drift dạng đối xứng (Stagger Recurrent Explicit) và một tập bán thực tế (Electricity semi-synthetic). Các tập tổng hợp được sinh bằng bộ sinh chuẩn của thư viện River~\cite{river2021}, một framework mã nguồn mở phổ biến trong cộng đồng data stream mining. Lý do dùng dữ liệu tổng hợp thay vì dữ liệu thực tế thuần là vì với dữ liệu thực, thời điểm xảy ra concept drift (ground truth) hầu như không biết được, nên không thể đo F1-Score hay Detection Delay. Bộ sinh chuẩn cho phép kiểm soát vị trí, cường độ và loại drift, giúp kết quả có thể tái lập.

\textit{Cấu hình đánh giá phát hiện.} Bộ đánh giá gồm 14 tập dữ liệu, mỗi tập chạy 30 seed độc lập. Hai tập được thêm để định nghĩa drift một cách chặt chẽ (dữ liệu recurrent dạng đối xứng A$\to$B$\to$A$\to$B\dots, đúng định nghĩa của Lu et al.~\cite{lu2018learning}) và dữ liệu chuyển tiếp tuyến tính trên một khoảng xác định trước. Một tập dữ liệu tĩnh đóng vai trò đo báo động giả. Số lần chạy là 30 lần đủ để chạy kiểm định Friedman/Nemenyi tại $\alpha = 0.05$ với độ ổn định cao hơn cho các chỉ số trung bình.

Các tập dữ liệu được phân loại dựa trên bản chất của sự thay đổi phân phối:

\paragraph{Nhóm 1: Sudden Drift (Thay đổi $P(X)$ đột ngột)}
Đây là trọng tâm chính của ShapeDD và IDW-MMD. Các dataset này chứa sự thay đổi đột ngột trong phân phối đầu vào $P(X)$ (Covariate Shift), nơi các phương pháp unsupervised cần phải phát hiện chính xác. Dạng drift này phản ánh các tình huống thực tế như: hệ thống IoT gặp sự cố cảm biến đột ngột, hoặc một dây chuyền sản xuất thay đổi nguyên liệu đầu vào.
\begin{itemize}
	\item \textit{Gaussian Shift (Moderate):} Dữ liệu phân phối chuẩn đa chiều ($d=10$) với sự dịch chuyển trung bình (mean shift) đột ngột ($\delta=1.5$). Đây là trường hợp lý tưởng để kiểm tra độ nhạy của kernel RBF, vì phân phối Gaussian là nền tảng của hầu hết các mô hình thống kê. Kịch bản này được thiết kế dựa trên phương pháp đánh giá tham số (parametric artificial datasets) trong bài khảo sát của Hinder et al.~\cite{hinder2024survey_partA}, nơi các tác giả sử dụng chúng để so sánh có hệ thống các phương pháp phát hiện drift unsupervised trên cùng một nền tảng thống nhất.

	\item \textit{STAGGER Concepts:} Bộ dữ liệu kinh điển được Schlimmer và Granger~\cite{schlimmer1986incremental} giới thiệu lần đầu năm 1986. Đây cũng là công trình đặt nền móng cho thuật ngữ ``concept drift'' trong lĩnh vực machine learning. Bộ dữ liệu gồm 3 đặc trưng (size, color, shape) với 3 giá trị mỗi đặc trưng, trong đó khái niệm phân lớp thay đổi theo các quy tắc Boolean khác nhau qua từng giai đoạn. Mặc dù thường được dùng cho supervised drift, nó cũng chứa sự thay đổi phân phối $P(X)$ do cách lấy mẫu, tạo ra tín hiệu drift rõ ràng.

	\item \textit{Random Uniform (Sensitivity Test):} Bộ 4 dataset (Random Mild, Random Moderate, Random Severe, Random Ultra Severe) được sinh ngẫu nhiên với cường độ thay đổi (intensity) tăng dần từ 0.125 đến 2.0, sử dụng bộ sinh dữ liệu của River~\cite{river2021}. Bộ này đóng vai trò như một ``nhiệt kế'' dùng để đánh giá ngưỡng phát hiện (detection threshold) của các thuật toán: drift ở mức Random Mild ($\delta=0.125$) gần như không thể phân biệt với nhiễu tự nhiên, trong khi Random Ultra Severe ($\delta=2.0$) tạo ra sự thay đổi rõ ràng mà bất kỳ thuật toán nào cũng phải phát hiện được.

	\item \textit{Stepping Drift (Cumulative):} Biến thể được bổ sung để kiểm tra khả năng thích ứng. Khác với Sudden Drift thông thường (thường luân phiên A$\to$B$\to$A), Stepping Drift dịch chuyển phân phối tích lũy theo một hướng (A$\to$B$\to$C$\to$D). Kịch bản này mô phỏng môi trường vận hành liên tục tiến hóa mà không quay lại trạng thái cũ (ví dụ trôi cảm biến công nghiệp), nên mô hình không thể ``học vẹt'' các trạng thái cũ mà buộc phải thích ứng với dữ liệu mới. \textit{Kịch bản này chỉ được dùng trong đánh giá thích ứng (Mục~\ref{sec:adaptation-eval}), không thuộc phần đánh giá phát hiện ở Mục~\ref{sec:detection-eval}.}
\end{itemize}

\paragraph{Nhóm 2: Blip Drift (Thay đổi ngắn hạn)}
Trong thực tế vận hành, không phải mọi sự thay đổi đều mang tính vĩnh viễn. Một đợt tấn công mạng ngắn hạn, một sự kiện khuyến mãi flash sale, hay một lỗi tạm thời của cảm biến đều tạo ra các ``xung nhiễu'' (blip) trong luồng dữ liệu rồi nhanh chóng quay lại trạng thái bình thường. Khả năng phân biệt blip với drift thực sự là thách thức lớn đối với các thuật toán phát hiện.
\begin{itemize}
	\item \textit{RBF Blips:} Dữ liệu được sinh từ bộ sinh Random RBF Generator~\cite{river2021} với 50 trọng tâm Gaussian (centroids) di chuyển đột ngột và quay lại trạng thái cũ trong thời gian ngắn. Cấu hình nhiều centroids tạo ra phân phối phức tạp, phi-Gaussian, gần với dữ liệu thực tế hơn so với Gaussian đơn giản. Thử thách khả năng phát hiện sự thay đổi tạm thời của các thuật toán.
\end{itemize}

\paragraph{Nhóm 3: Real Drift / Thay đổi ranh giới quyết định (Control Group)}
Nhóm này đóng vai trò \textbf{nhóm đối chứng} (negative control), gồm các dataset chỉ có sự thay đổi ở biên quyết định $P(Y|X)$ còn phân phối đầu vào $P(X)$ giữ nguyên (hoặc thay đổi không đáng kể). Đây là loại ``virtual drift'' mà các phương pháp unsupervised (chỉ quan sát $P(X)$) \textit{không nên} phát hiện. Benchmark trên nhóm này giúp xác nhận thuật toán không bị ``ảo giác'' (hallucination) trước các thay đổi chỉ mang tính ngữ nghĩa nhãn.
\begin{itemize}
	\item \textit{Standard SEA:} Bộ dữ liệu benchmark do Street và Kim đề xuất tại hội nghị ACM SIGKDD 2001~\cite{street2001streaming}. SEA (Streaming Ensemble Algorithm) ban đầu được thiết kế để đánh giá các phương pháp ensemble learning cho luồng dữ liệu. Bộ dữ liệu gồm 3 đặc trưng ngẫu nhiên, trong đó chỉ 2 đặc trưng đầu là liên quan; drift được tạo bằng cách thay đổi ngưỡng phân lớp $x_1 + x_2 > \theta$ (thường dùng các ngưỡng 8, 9, 7, 9.5). Đây là một trong những benchmark được trích dẫn nhiều nhất trong lĩnh vực concept drift.

	\item \textit{Rotating Hyperplane:} Bộ dữ liệu do Hulten, Spencer và Domingos giới thiệu tại KDD 2001~\cite{hulten2001mining} trong bối cảnh nghiên cứu khai phá luồng dữ liệu biến đổi theo thời gian (mining time-changing data streams). Mặt siêu phẳng $d$-chiều xoay dần trong không gian bằng cách thay đổi trọng số của các đặc trưng, mô phỏng gradual drift, dạng drift mà ranh giới quyết định dịch chuyển từ từ tương tự như sự thay đổi dần dần trong hành vi người dùng hay điều kiện thị trường.

	\item \textit{LED Abrupt:} Bài toán phân lớp chữ số trên đèn LED 7 thanh, được sinh bằng bộ sinh chuẩn của River~\cite{river2021}. Bộ dữ liệu gồm 24 thuộc tính nhị phân (7 relevant, 17 nhiễu), mỗi thuộc tính có 10\% xác suất bị đảo ngược. Drift được tạo bằng cách hoán đổi vị trí các thuộc tính relevant tại các mốc thời gian xác định, tạo ra sự thay đổi đột ngột trong quy tắc phân lớp mà không ảnh hưởng đến phân phối tổng thể $P(X)$.
\end{itemize}

\paragraph{Nhóm 4: Dữ liệu bán thực tế (Semi-Synthetic) với điểm drift kiểm soát}
Nhóm này bắc cầu giữa dữ liệu tổng hợp và dữ liệu thực tế thuần túy. Dataset lấy đặc trưng đầu vào từ dữ liệu thực (phức tạp, phi-Gaussian, có tương quan giữa các chiều) còn điểm drift thì được inject nhân tạo để có ground truth rõ ràng. Cách này cho phép kiểm tra hành vi của thuật toán trên phân phối phức tạp mà vẫn đo được các chỉ số đánh giá.
\begin{itemize}
	\item \textit{Electricity Semi-Synthetic:} Dựa trên bộ dữ liệu Electricity gốc của Harries~\cite{harries1999splice} từ thị trường điện năng bang New South Wales, Australia (giai đoạn 1996--1998, 45,312 quan sát). Bộ dữ liệu gốc ghi nhận giá điện dao động theo thời gian thực với các đặc trưng như: nhu cầu điện năng, lượng chuyển giao liên bang, và chu kỳ thời gian. Đây là một trong những benchmark thực tế được sử dụng phổ biến nhất trong nghiên cứu concept drift, do bản chất dữ liệu vốn đã chứa các yếu tố biến động tự nhiên (mùa vụ, sự kiện thị trường). Trong phiên bản semi-synthetic, 10 điểm drift được chèn tại các vị trí cố định bằng cách xáo trộn phân phối đặc trưng, cho phép kiểm tra hành vi của thuật toán trên dữ liệu phân phối phi-Gaussian phức tạp trong khi vẫn có ground truth xác định.
\end{itemize}

\paragraph{Nhóm 5: Recurrent, Gradual, và Stationary (mở rộng theo lý thuyết)}
Ba dataset trong nhóm này được thêm để định nghĩa Recurrent, Gradual và Stationary một cách chặt chẽ về mặt lý thuyết, đồng bộ với các định nghĩa drift trong khảo sát của Lu et al.~\cite{lu2018learning}.
\begin{itemize}
	\item \textit{STAGGER Recurrent (Explicit):} Bộ STAGGER được lặp lại theo dạng đối xứng A$\to$B$\to$A$\to$B\dots\ thay vì coi recurrent là ``sudden lặp''. Mỗi concept được giữ ổn định một khoảng thời gian, sau đó quay về concept đã từng xuất hiện. Đây là kịch bản đúng với định nghĩa Recurrent trong khảo sát của Lu et al.~\cite{lu2018learning}, cho phép Concept Memory (Mục~\ref{sec:se-cdt-concept-memory}) phát huy đúng vai trò.
	\item \textit{Gaussian Gradual Synthetic:} Dữ liệu Gaussian đa chiều với chuyển tiếp tuyến tính giữa hai mean trên một khoảng chuyển tiếp xác định trước (mặc định 200 mẫu). Khác với hyperplane (drift gradual chỉ ở $P(Y|X)$), dataset này cho drift gradual thực sự ở $P(X)$, do đó đo được khả năng phát hiện drift chậm của các phương pháp không giám sát. Đây cũng là điểm yếu cố hữu của IDW-MMD và là một trường hợp khó quan trọng để kiểm tra giới hạn của phương pháp.
	\item \textit{STAGGER None:} STAGGER stationary, không có drift. Tập này đóng vai trò đo báo động giả trên dữ liệu tĩnh, bổ sung cho kiểm tra Type-I error ở Mục~\ref{sec:h0-calibration}: nó cho biết liệu detector có sinh báo động giả khi luồng dữ liệu hoàn toàn không đổi hay không.
\end{itemize}


\subsection{Chiến lược đánh giá}
\label{sec:eval-strategy}

Trước khi so sánh với các phương pháp hiện có, cần làm rõ kiến trúc của hệ thống đề xuất để định hình chiến lược đánh giá. Hệ thống SE-CDT được thiết kế theo kiến trúc modular gồm ba thành phần chính tương ứng với 3 giai đoạn xử lý:

\begin{enumerate}
	\item \textbf{Detection Module (ShapeDD-IDW):}
	Kết hợp ShapeDD với IDW-MMD và Gamma-approximation p-value (xem Mục~\ref{sec:detection-hybrid}) để phát hiện thời điểm xảy ra drift. Module này ưu tiên giảm thiểu False Positive Rate (FP) và độ trễ.
	
	\item \textbf{Classification Module (SE-CDT):}
	Phân loại loại drift (sudden, gradual, incremental, recurrent, blip) dựa trên tín hiệu drift magnitude $\sigma(t)$. SE-CDT hoạt động hoàn toàn \textit{không giám sát} bằng cách phân tích hình dạng của $\sigma(t)$ trên các cửa sổ trượt đa kích thước.
	
	\item \textbf{Adaptation Module:}
	Kích hoạt chiến lược thích ứng phù hợp dựa trên kết quả classification: Full Reset (sudden), Incremental Update (gradual/incremental), Concept Memory (recurrent).
\end{enumerate}

\paragraph{Quy ước về độ trễ chấp nhận ($\delta$).}
\label{par:tolerance-convention}
Ba mục đánh giá tiếp theo dùng ba giá trị $\delta$ khác nhau, tùy theo bản chất của bài toán mỗi mục đo. Sự khác biệt này được công bố ở đây để người đọc dễ đối chiếu các con số:
\begin{itemize}
	\item \textbf{Mục~\ref{sec:detection-eval} (Detection benchmark):} $\delta = 75$ mẫu (cửa sổ đối xứng). Mục tiêu là đo \textit{độ chính xác định vị} của detector. Drift trong benchmark này là dạng abrupt (chiều rộng $0$ với Sudden, hoặc chuyển tiếp ngắn) nên ngưỡng tương đối chặt là phù hợp.
	\item \textbf{Mục~\ref{sec:classification-eval} (Classification benchmark):} $\delta = 250$ mẫu, kèm $\delta_{\text{early}} = 50$. Bộ sinh dữ liệu của benchmark này tạo các sự kiện kéo dài (Gradual width $= 400$, Recurrent period $= 200$, Incremental width $= 600$), do đó việc đòi hỏi định vị trong $\pm 75$ mẫu là không thực tế --- detector có thể báo drift ở bất kỳ thời điểm nào trong khoảng chuyển tiếp đó. Ngưỡng $250$ mẫu cho phép chấp nhận các báo phát hiện rơi trong khoảng chuyển tiếp đó như đúng sự kiện.
	\item \textbf{Mục~\ref{sec:adaptation-eval} (Adaptation benchmark):} $\delta = 400$ mẫu. Mục tiêu là đo \textit{khả năng phục hồi} mô hình sau drift, không phải độ chính xác định vị. Detector chỉ cần báo \textit{trong khoảng thời gian} mà mô hình bị suy giảm hiệu suất là đủ để adaptation kích hoạt; do đó ngưỡng rộng hơn được chấp nhận.
\end{itemize}
Quy ước này thể hiện rằng cùng một detector có thể có $\text{EDR}/\text{FP}$ khác nhau ở ba mục, không phải vì thuật toán thay đổi mà vì mục đo đang trả lời câu hỏi khác. Khi cần so sánh trực tiếp giữa các mục, cần lưu ý sự khác biệt này.

\section{Đánh giá phương pháp phát hiện Drift}
\label{sec:detection-eval}

Mục này tập trung đối chiếu hiệu năng của Detection Module so với các phương pháp tiên tiến đại diện cho các trường phái khác nhau: KS-Test (thống kê cổ điển), MMD (kernel-based), DAWIDD (window-based), D3 (discriminative classifier), và ShapeDD gốc (2021). Trọng tâm phân tích được đặt vào khả năng nhận diện \textbf{Sự thay đổi phân phối đầu vào} $P(X)$ (Distribution Shift / Covariate Shift).

\subsection{Thiết lập thực nghiệm (Detection Setup)}
Các tham số cho bài toán benchmark phát hiện drift được thiết lập như sau:
\begin{itemize}
	\item \textbf{Dữ liệu:} 14 tập dữ liệu (13 tổng hợp + 1 bán thực tế, chi tiết ở Mục~\ref{sec:dataset-config}). Tập stationary (không có drift) được tách riêng để đo false-positive.
	\item \textbf{Kích thước luồng:} $T = 10{,}000$ mẫu mỗi dataset.
	\item \textbf{Số lượng drift:} 10 điểm drift được chèn tại các vị trí cố định (mỗi 1{,}000 mẫu) để tạo ground truth, trừ tập stationary (0 drift).
	\item \textbf{Quy trình đánh giá:} Thực hiện 30 lần chạy độc lập (30 runs) với các random seed khác nhau (xem Mục~\ref{sec:dataset-config}).
	\item \textbf{Độ trễ chấp nhận ($\delta$):} 75 mẫu. Một phát hiện được tính là True Positive nếu $|t_{det} - t_{drift}| \leq \delta$ (cửa sổ đối xứng), ngược lại được tính là False Positive.
\end{itemize}

\subsection{Kết quả so sánh tổng hợp}

Bảng~\ref{tab:comprehensive_performance} so sánh hiệu suất của \textbf{phương pháp đề xuất} (IDW-MMD, ShapeDD-IDW, SE-CDT) với \textbf{các baseline} (Standard MMD, KS-Test, D3, DAWIDD, ShapeDD gốc). Các chỉ số F1 tính dựa trên khả năng phát hiện đúng các điểm drift đã biết (ground truth), tổng hợp từ 30 lần chạy độc lập trên 14 tập dữ liệu.

% =============================================================================
% Bảng tổng hợp hiệu suất
% =============================================================================
\begin{table}[H]
	\centering
	\caption{So sánh tổng hợp hiệu suất các phương pháp phát hiện drift}
	\label{tab:comprehensive_performance}
	\input{../../results/tables/table_I_comprehensive_performance.tex}
\end{table}

\begin{itemize}
	\item \textbf{IDW-MMD, ShapeDD-IDW và SE-CDT cùng đạt F1 = 0.459:} Ba phương pháp đề xuất cho kết quả hoàn toàn như nhau trên mọi tập dữ liệu — Precision $= 0.346$, Recall/EDR $= 0.760$ và $14.5$ báo động giả trung bình mỗi run — vì cả ba đều dùng chung bộ kiểm định IDW-MMD với p-value xấp xỉ Gamma (\texttt{wmmd\_gamma}). Cấu hình này thiên về Recall cao, phù hợp các ứng dụng giám sát nơi bỏ sót drift tốn kém hơn xử lý báo nhầm. F1 $= 0.459$ cao hơn ShapeDD gốc dùng permutation test ($F_1 = 0.432$, 18.4 FP). Thời gian chạy của ShapeDD-IDW nhanh hơn ShapeDD gốc gần $6\times$ (Bảng~\ref{tab:runtime_stats}).
	\item \textbf{D3 dẫn đầu về F1 (0.492):} Phương pháp discriminative này hoạt động tốt với Precision cao (0.555) và FP thấp (0.8), nhưng Recall hạn chế (0.479) do phụ thuộc vào khả năng học của classifier.
	\item \textbf{MMD chuẩn (F1 = 0.465):} Phương pháp MMD chuẩn (không có bước phát hiện theo hình dạng) đạt Recall $= 0.760$, Precision $= 0.351$, với 15.0 FP/run. Số liệu này gần với ba phương pháp đề xuất vì đều dựa trên tín hiệu MMD theo cửa sổ trượt. Khác biệt nhỏ về FP (15.0 → 14.5) đến từ bước lọc hình dạng và kiểm định IDW-MMD tại đỉnh ứng viên.
	\item \textbf{Sự đánh đổi của KS-Test (F1 = 0.294):} Phương pháp Kolmogorov-Smirnov đạt Recall cao nhất (0.826) nhưng lại có Precision thấp nhất (0.189) với số lượng báo động giả lớn (37.8 FP/run). Điều này cho thấy KS quá nhạy cảm với nhiễu, không phù hợp cho các hệ thống giám sát tự động yêu cầu độ tin cậy cao.
\end{itemize}

\textbf{Diễn giải số False Positive.} Số FP $\sim 14.5$/run của SE-CDT/ShapeDD-IDW/IDW-MMD cần được đọc cùng số lượng cửa sổ và số ứng viên được kiểm định. Trên bộ đánh giá này, mỗi seed có nhiều cửa sổ kiểm định và nhiều điểm drift thật. Bước lọc hình dạng giúp giảm số ứng viên trước khi kiểm định bằng IDW-MMD, vì vậy FP thực tế thấp hơn trường hợp kiểm định mọi cửa sổ một cách độc lập. Tuy nhiên, nếu muốn giảm FP hơn nữa, cần siết mức $\alpha$ hoặc dùng hiệu chỉnh đa kiểm định.

\subsection{Phân tích ý nghĩa thống kê}

Để kiểm tra liệu khác biệt hiệu suất giữa các phương pháp có ý nghĩa thống kê hay không, Hình~\ref{fig:critical_difference} trình bày biểu đồ Critical Difference (CD) dùng kiểm định Friedman và Nemenyi post-hoc.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{../../results/plots/critical_difference_f1.png}
	\caption[Biểu đồ Critical Difference (CD)]{Biểu đồ Critical Difference (CD) với mức ý nghĩa $\alpha = 0.05$. SE\_CDT và nhóm dẫn đầu (MMD, MMD\_IDW) không khác biệt có ý nghĩa thống kê.}
	\label{fig:critical_difference}
\end{figure}

Kết quả kiểm định Friedman cho thấy MMD cơ bản có thứ hạng trung bình tốt nhất (3.679), tiếp theo là IDW-MMD (4.107) và DAWIDD (4.179). SE-CDT xếp hạng 5 (4.643, đồng hạng với ShapeDD-IDW theo bảng thống kê). Với ngưỡng Critical Difference CD = 2.806 ($\alpha = 0.05$), hai phương pháp được coi là không khác biệt thống kê khi khoảng cách thứ hạng nhỏ hơn CD. Khoảng cách giữa SE-CDT và phương pháp dẫn đầu (MMD) là $4.643 - 3.679 = 0.964 < 2.806$, do đó về F1 thì các phương pháp nhóm dẫn đầu không khác biệt có ý nghĩa thống kê.

SE-CDT và ShapeDD-IDW có số FP trung bình thấp hơn ShapeDD gốc (Bảng~\ref{tab:comprehensive_performance}); hành vi của bước kiểm định được phân tích thêm ở Mục~\ref{sec:h0-calibration}.

\subsection{Đánh giá chi tiết trên từng loại dữ liệu}

Hiệu suất của các phương pháp thay đổi tùy thuộc vào đặc tính của tập dữ liệu (Bảng~\ref{tab:f1_by_dataset_part1}--\ref{tab:f1_by_dataset_part4}).

% =============================================================================
% Bảng F1 theo Dataset
% =============================================================================
\begin{table}[H]
	\centering
	\caption{F1-Score theo dataset (Phần 1: Dữ liệu cơ bản và Mild/Moderate Drift)}
	\label{tab:f1_by_dataset_part1}
	\input{../../results/tables/table_II_part1.tex}
\end{table}

\begin{table}[H]
	\centering
	\caption{F1-Score theo dataset (Phần 2: Drift mạnh và thay đổi mặt phẳng)}
	\label{tab:f1_by_dataset_part2}
	\input{../../results/tables/table_II_part2.tex}
\end{table}

\begin{table}[H]
	\centering
	\caption{F1-Score theo dataset (Phần 3: Blip và dữ liệu Stagger/SEA)}
	\label{tab:f1_by_dataset_part3}
	\input{../../results/tables/table_II_part3.tex}
\end{table}

\begin{table}[H]
	\centering
	\caption{F1-Score theo dataset (Phần 4: Standard SEA và trung bình)}
	\label{tab:f1_by_dataset_part4}
	\input{../../results/tables/table_II_part4.tex}
\end{table}

\begin{enumerate}
	\item \textbf{Kết quả trên các drift rõ:}
	      Trên các tập có thay đổi phân phối rõ như Random Moderate, Random Severe, Random Ultra Severe, Stagger và Stagger Recurrent Explicit, ba phương pháp đề xuất (IDW-MMD, ShapeDD-IDW, SE-CDT) đồng loạt đạt F1 khoảng $0.57$--$0.69$, ổn định hơn ShapeDD gốc ở phần lớn các trường hợp và giữ được tín hiệu Standard MMD cần cho bước phân loại. Hình~\ref{fig:sudden_drift_vis} minh họa dạng tín hiệu ShapeDD tại một kịch bản sudden drift.
	\item \textbf{Drift nhẹ và drift dần dần:}
	      Trên Random Mild, ba phương pháp đề xuất đạt F1 $= 0.525$, thấp hơn MMD chuẩn ($0.578$) nhưng không bị mất hoàn toàn tín hiệu. Với Gaussian Gradual Synthetic, cả ba phương pháp đề xuất đạt F1 $= 0.429$, tương đương MMD chuẩn ($0.429$). Điều này cho thấy bước kiểm định IDW-MMD với p-value Gamma đủ nhạy để phát hiện drift dần dần khi cửa sổ tham chiếu và cửa sổ kiểm tra đủ lớn ($l_1 = 50$, $l_2 = 150$).
	\item \textbf{Nhóm thay đổi chủ yếu ở nhãn:}
	      Trên các tập Hyperplane, Led Abrupt, và Standard Sea, điểm F1 của SE-CDT/ShapeDD-IDW tương đối thấp ($0.194$, $0.126$, và $0.122$). Đây là hành vi phù hợp với phạm vi của phương pháp không giám sát: hệ thống chủ yếu quan sát $P(X)$, nên các thay đổi nằm ở $P(Y|X)$ không tạo tín hiệu rõ. Tuy nhiên, các giá trị khác 0 cũng cho thấy vẫn còn một mức báo động dư thừa do nhiễu mẫu và do cách đánh giá theo cửa sổ.
	\item \textbf{Blip Drift:}
	      Trên tập RBF Blips, ba phương pháp đề xuất đạt F1 $= 0.598$ ở bài toán phát hiện, gần với MMD chuẩn ($0.602$), trong khi D3 đạt cao hơn hẳn ($1.000$) nhờ classifier phân biệt cửa sổ trước và sau blip. Điều này cho thấy các thay đổi ngắn hạn có thể được phát hiện, nhưng việc phân loại đúng Blip vẫn còn khó; hạn chế này được phân tích riêng ở Mục~\ref{sec:classification-by-type}.
	\item \textbf{Báo động giả (False Positives trong detection):}
	      Hình~\ref{fig:stationary_fp} so sánh số FP trong bộ đánh giá phát hiện. KS-Test ghi nhận nhiều báo động sai nhất (37.8 FP/run); ba phương pháp đề xuất (SE-CDT, ShapeDD-IDW, IDW-MMD) giữ FP ở mức trung bình $\sim 14.5$/run, thấp hơn ShapeDD gốc (18.4 FP/run) và gần MMD chuẩn (15.0 FP/run). Có hai lý do giúp bước lọc này phát huy tác dụng: (i) cơ chế phát hiện hình dạng yêu cầu tín hiệu MMD phải khớp dạng tam giác đặc trưng; (ii) băng thông kernel $\gamma$ được ước lượng một lần bằng median heuristic và dùng chung cho cả ba kernel $K_{XX}, K_{YY}, K_{XY}$.
\end{enumerate}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{../../results/plots/vis_mixed_a_SE.png}
	\caption[Minh họa tín hiệu phát hiện trên dữ liệu Sudden Drift]{Minh họa tín hiệu phát hiện trên dữ liệu Sudden Drift (kịch bản Mixed A). ShapeDD tạo ra hình dạng "tam giác cân" đặc trưng tại điểm drift, đúng như dự báo lý thuyết.}
	\label{fig:sudden_drift_vis}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{../../results/plots/vis_repeated_gradual_SE.png}
	\caption[Minh họa tín hiệu trên dữ liệu Gradual Drift]{Minh họa tín hiệu trên dữ liệu Gradual Drift. Tín hiệu (màu đỏ - Subplot 03) không tạo thành đỉnh nhọn rõ ràng mà bị san phẳng.}
	\label{fig:gradual_drift_vis}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{../../results/plots/figure_4_stationary_fp.png}
	\caption[Phân tích số lượng báo động giả trên tập dữ liệu tĩnh]{Phân tích số lượng báo động giả trên tập dữ liệu tĩnh (Stationary). KS tạo ra nhiều báo động sai nhất, trong khi các phương pháp MMD và D3 ổn định hơn.}
	\label{fig:stationary_fp}
\end{figure}

\subsection{Kiểm tra tỉ lệ báo động sai trên dữ liệu không có drift}
\label{sec:h0-calibration}

Số False Positive quan sát được trên bộ đánh giá phát hiện chỉ phản ánh một phần hành vi của kiểm định, vì các mốc drift thật tương đối thưa và bước lọc theo hình dạng đỉnh đã loại bớt ứng viên trước khi kiểm định. Để đánh giá trực tiếp liệu kiểm định có thực sự gần mức $\alpha = 0.05$ hay không, luận văn tiến hành thêm một thí nghiệm phụ trên dữ liệu \textit{hoàn toàn không có drift} và đo trực tiếp \textbf{Type-I error} (xác suất báo động sai khi giả thuyết H0 đúng) của ba bộ phát hiện chính:
\begin{itemize}
	\item \textbf{MMD chuẩn} với p-value tính bằng permutation test ($N_{\text{perm}} = 2{,}500$).
	\item \textbf{IDW-MMD} với p-value tính theo xấp xỉ Gamma.
	\item \textbf{SE-CDT} (kết hợp ShapeDD-IDW cho phát hiện và decision tree cho phân loại).
\end{itemize}

\textbf{Thiết lập.} Mỗi detector chạy trên ba phân phối tĩnh để bao quát các tình huống thường gặp: (i) Gauss-iid $d=5$, (ii) Gauss-iid $d=10$ (kiểm tra ảnh hưởng số chiều), và (iii) Gauss-AR(1) $d=5$ với $\rho = 0.5$ (kiểm tra dữ liệu có tương quan thời gian). Mỗi cấu hình chạy độc lập 200 lần với seed khác nhau, dùng cửa sổ tham chiếu $n = 300$ và cửa sổ test $m = 150$ với $l_1 = 50$. Mức ý nghĩa danh nghĩa $\alpha = 0.05$.

\begin{table}[H]
	\centering
	\caption{Tỉ lệ Type-I error trên dữ liệu tĩnh (200 lần chạy mỗi cell, $\alpha = 0.05$). Số sau dấu $\pm$ là nửa khoảng tin cậy 95\% (Wilson); các giá trị cần được đọc cùng khoảng tin cậy và cơ chế kiểm định của từng detector.}
	\label{tab:h0_calibration}
	\input{../../results/tables/table_h0_calibration.tex}
\end{table}

\textbf{Phân tích.} Kết quả ở Bảng~\ref{tab:h0_calibration} cho thấy:
\begin{itemize}
	\item MMD chuẩn (permutation test, $N_{\text{perm}} = 2{,}500$) có Type-I error nằm trong khoảng $0.025$--$0.055$, sát với mức danh nghĩa $\alpha = 0.05$. Đây là hành vi đúng với lý thuyết, cho thấy MMD chuẩn dùng làm baseline trong luận văn không bị thiên lệch.
	\item IDW-MMD với xấp xỉ Gamma đạt Type-I error trong khoảng $0.040$--$0.075$, gần với mức danh nghĩa $\alpha = 0.05$. Kiểm định do đó không quá bảo thủ như xấp xỉ Gaussian dưới giả thuyết không drift.
	\item SE-CDT (kết hợp bước lọc hình dạng và IDW-MMD ở bước kiểm định) đạt Type-I error $0.045$--$0.100$, hơi cao hơn $\alpha$ trên hai phân phối ($d=5$ iid và AR(1)). Hiện tượng này xuất phát từ việc nhiều đỉnh ứng viên có thể được kiểm định trong cùng một cửa sổ. Mỗi đỉnh đều dùng $\alpha = 0.05$ riêng, dẫn đến family-wise error rate hơi tăng. Đây là vấn đề kiểm định đa giả thuyết đã biết, có thể giảm bằng hiệu chỉnh Bonferroni theo số đỉnh (hướng cải tiến trong tương lai). Trong bộ đánh giá phát hiện, hiệu ứng này được bù đắp một phần bởi bước lọc theo hình dạng đỉnh, nên FP đo được không tăng theo tỉ lệ tương ứng.
\end{itemize}

Việc chọn xấp xỉ Gamma theo Gretton et al.~\cite{gretton2009gamma} thay cho xấp xỉ Gaussian xuất phát từ một nhận xét lý thuyết: dưới giả thuyết không drift, MMD$^2_u$ tiệm cận một $\chi^2$ có trọng chứ không phải Gaussian. Nếu áp dụng Gaussian dưới giả thuyết không drift, kiểm định có xu hướng quá bảo thủ. Xấp xỉ Gamma khắc phục điểm này tốt hơn và cho kết quả gần mức $\alpha = 0.05$ hơn trong thí nghiệm trên dữ liệu tĩnh.

\subsection{Đánh giá hiệu suất tính toán}

Bảng~\ref{tab:runtime_stats} so sánh thời gian và thông lượng.

\begin{table}[H]
	\centering
	\caption{So sánh hiệu suất tính toán của các phương pháp}
	\input{../../results/tables/table_III_runtime_stats.tex}
\end{table}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{../../results/plots/figure_6_runtime.png}
	\caption{Thời gian chạy trung bình của các phương pháp trên đánh giá phát hiện (10,000 mẫu, 30 lần chạy).}
	\label{fig:runtime-comparison}
\end{figure}

Kết quả nổi bật là ShapeDD gốc, ShapeDD-IDW, SE-CDT và MMD chuẩn đều có thời gian chạy cùng cỡ trên luồng 10{,}000 mẫu (Bảng~\ref{tab:runtime_stats}). ShapeDD gốc đã được tối ưu khá tốt nhờ tính kernel theo lô, nên việc thay permutation test bằng xấp xỉ Gamma không làm thời gian chạy giảm mạnh, nhưng cũng không làm hệ thống chậm hơn. Đóng góp chính của ShapeDD-IDW trong thí nghiệm này là giữ được thời gian chạy tương đương ShapeDD gốc trong khi bước kiểm định có cơ sở thống kê rõ hơn (Mục~\ref{sec:h0-calibration}).

\section{Đánh giá SE-CDT phân loại drift không giám sát}
\label{sec:classification-eval}

Phần này đánh giá Classification Module (SE-CDT): khả năng nhận diện loại drift (Sudden, Gradual, Incremental, Recurrent, Blip) dựa trên hình dạng tín hiệu drift magnitude $\sigma(t)$, hoàn toàn không giám sát.

\subsection{Thiết lập thực nghiệm}
\label{sec:classification-setup}

\paragraph{Hai chế độ đánh giá.}
Benchmark phân loại dùng song song hai chế độ đo lường, ứng với hai câu hỏi khác nhau:
\begin{itemize}
	\item \textbf{Oracle mode} (đo CAT, SUB): SE-CDT được cho biết trước vị trí drift ground-truth và chỉ thực hiện bước phân loại. Chế độ này tách biệt năng lực phân biệt hình dạng tín hiệu khỏi năng lực tự phát hiện.
	\item \textbf{Real detection mode} (đo EDR, MDR): SE-CDT tự tìm điểm drift từ tín hiệu MMD rồi phân loại, đánh giá pipeline đầu-cuối. EDR (Event Detection Rate) là tỉ lệ sự kiện drift được nhận diện; MDR (Miss Detection Rate $= 1 - \text{EDR}$) là tỉ lệ bỏ sót.
\end{itemize}
Vì CAT và SUB được đo ở oracle mode, hai cấu hình SE-CDT (Std) và SE-CDT (IDW) cho cùng kết quả CAT/SUB. Khác biệt giữa hai cấu hình chỉ xuất hiện ở EDR/MDR.

\paragraph{Kịch bản thực nghiệm.}
Benchmark chạy 6 kịch bản tổng hợp (30 seeds mỗi kịch bản = 180 luồng, mỗi luồng chứa 10 sự kiện drift = \textbf{1800 sự kiện phân loại}):

\begin{table}[H]
	\centering
	\caption{Sáu kịch bản benchmark phân loại SE-CDT}
	\label{tab:classification-scenarios}
	\begin{tabular}{|l|l|c|}
		\hline
		\textbf{Kịch bản} & \textbf{Thành phần drift (xoay vòng)} & \textbf{Độ dài luồng} \\
		\hline
		Mixed A        & Sudden $\to$ Gradual $\to$ Recurrent   & $\sim$11,800 mẫu \\
		Mixed B        & Blip $\to$ Incremental $\to$ Sudden    & $\sim$13,800 mẫu \\
		Repeated Sudden      & 10 $\times$ Sudden                     & $\sim$9,600 mẫu  \\
		Repeated Gradual     & 10 $\times$ Gradual                    & $\sim$11,800 mẫu \\
		Repeated Incremental & 10 $\times$ Incremental                & $\sim$13,800 mẫu \\
		Repeated Recurrent   & 10 $\times$ Recurrent (A$\to$B$\to$A)  & $\sim$11,800 mẫu \\
		\hline
	\end{tabular}
\end{table}

\paragraph{Bộ sinh dữ liệu.} Mỗi sự kiện Sudden, Gradual và Incremental dịch chuyển một tập đặc trưng khác nhau theo cơ chế xoay vòng, để mỗi concept mới là độc lập với các concept đã thấy, đúng theo định nghĩa của Lu và cộng sự~\cite{lu2018learning}. Riêng Recurrent giữ logic luân phiên $A \leftrightarrow B$ vì đây mới là định nghĩa đặc trưng của loại drift này.

\paragraph{Cấu hình tham số.}
\begin{itemize}
	\item \textbf{Cửa sổ ShapeDD:} $W = 50$, stride $s = 10$.
	\item \textbf{Tín hiệu phân loại:} Standard MMD. Các thử nghiệm so sánh cho thấy IDW-MMD không phù hợp làm tín hiệu phân loại vì dễ làm mờ các drift chậm như Gradual/Incremental. Vì vậy hệ thống dùng mô hình kết hợp: bước kiểm định ứng viên dùng IDW-MMD, còn bước phân loại dùng Standard MMD để giữ lại thông tin hình dạng toàn cục.
	\item \textbf{Ngưỡng phát hiện:} hiệu chỉnh trên một tập validation riêng (xem Bảng cấu hình tham số ở Phụ lục). Ngưỡng mặc định của thư viện gốc quá bảo thủ trong điều kiện streaming nên đã được nới xuống cho phù hợp.
\end{itemize}

\paragraph{Hình dạng tín hiệu.}
Trước khi đọc kết quả số, Hình~\ref{fig:signal_sudden} và Hình~\ref{fig:signal_incremental} minh họa trực quan lý do Sudden drift dễ phân loại hơn Incremental: SE-CDT dựa vào sự khác biệt hình dạng của tín hiệu $\sigma(t)$ để phân biệt các loại drift.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{../../results/plots/vis_repeated_sudden_SE.png}
	\caption[Hình dạng tín hiệu SE-CDT --- Sudden Drift]{Tín hiệu SE-CDT trên kịch bản Repeated Sudden: $\sigma(t)$ tạo đỉnh nhọn, đối xứng tại mỗi điểm drift --- đặc trưng dễ nhận diện bằng height threshold.}
	\label{fig:signal_sudden}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{../../results/plots/vis_repeated_incremental_SE.png}
	\caption[Hình dạng tín hiệu SE-CDT --- Incremental Drift]{Tín hiệu SE-CDT trên kịch bản Repeated Incremental: $\sigma(t)$ tăng dần theo dạng ramp, không có đỉnh nhọn --- khó phân biệt với Gradual drift, phản ánh độ chính xác subcategory thấp hơn (23.8\%).}
	\label{fig:signal_incremental}
\end{figure}

\subsection{Kết quả tổng hợp}
\label{sec:classification-results}

\textit{Lưu ý về thuật ngữ FP.} Ở phần detection (Mục~\ref{sec:detection-eval}), FP là số lần báo động sai trên dữ liệu không có drift. Ở đây, FP là số lần SE-CDT kích hoạt phân loại tại vị trí không khớp với bất kỳ điểm drift nào trong ground truth. Hai khái niệm khác nhau.

Bảng~\ref{tab:se-cdt-results} trình bày kết quả tổng hợp trên 1800 sự kiện phân loại (180 luồng $\times$ 10 sự kiện). Hai cấu hình đều dùng Standard MMD cho tín hiệu phân loại nên CAT/SUB bằng nhau, khác biệt chỉ nằm ở tín hiệu detection. SE-CDT (Std) dùng Standard MMD, nhạy với cả 5 loại drift (EDR $\approx 92.6\%$) nhưng FP cao hơn. SE-CDT (IDW) dùng IDW-MMD, chặt hơn về FP nhưng bỏ sót Gradual/Incremental. Trong ngữ cảnh phân loại, ưu tiên là không bỏ sót sự kiện, vì vậy \textbf{SE-CDT (Std)} được chọn làm cấu hình chính.

\begin{table}[H]
	\centering
	\caption{Kết quả phân loại drift type của SE-CDT (tổng hợp 1800 sự kiện, 6 kịch bản $\times$ 30 seeds)}
	\label{tab:se-cdt-results}
	\input{../../results/tables/table_se_cdt_aggregate.tex}
\end{table}

SE-CDT (Std) đạt \textbf{CAT $= 81.9\%$} và \textbf{SUB micro-average $= 65.8\%$} ở oracle mode. Vì micro-average bị chi phối bởi lớp đông nhất (Sudden), bảng \ref{tab:se-cdt-performance-by-type} cũng báo cáo \textbf{macro-average per-class $= 55.7\%$} để phản ánh độ cân bằng giữa 5 nhóm drift. Khi đọc kết quả trong các phần sau, cần lưu ý sự khác biệt giữa hai metric này: macro-average mới phản ánh đúng độ cân bằng của bộ phân loại trên 5 nhóm drift, trong khi micro-average tỏ ra cao hơn nhờ phân phối lớp lệch về Sudden và Recurrent trong các kịch bản benchmark.

Khi không có Concept Memory, Recurrent drift bị xử lý như một chuỗi sự kiện Sudden độc lập và độ chính xác là $0\%$. Sau khi bổ sung Concept Memory (so khớp ring buffer bằng Standard MMD), độ chính xác Recurrent đạt \textbf{89.0\%}, vượt cả CDT-MSW gốc ($83.9\%$ trên benchmark UCI khác). Kết quả này cho thấy Concept Memory hoạt động đúng nguyên lý khi dữ liệu đầu vào tuân theo đúng định nghĩa Recurrent của Lu et al.~\cite{lu2018learning}.

\subsection{Phân tích chi tiết theo loại drift}
\label{sec:classification-by-type}

Bảng~\ref{tab:se-cdt-performance-by-type} và Hình~\ref{fig:confusion-5class}, \ref{fig:confusion-category} phân tích nguồn gốc lỗi phân loại.

\begin{table}[H]
	\centering
	\caption{Độ chính xác phân loại theo từng loại drift (oracle mode, SE-CDT Std)}
	\label{tab:se-cdt-performance-by-type}
	\input{../../results/tables/table_se_cdt_performance_by_type.tex}
\end{table}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{../../results/plots/confusion_matrix_se_cdt.png}
	\caption[Confusion Matrix --- 5 Classes]{Confusion Matrix cho 5 loại drift (oracle mode, cấu hình SE-CDT đầy đủ với Concept Memory và Online Self-Calibration). Sudden $\approx 82\%$, Recurrent $\approx 89\%$, Incremental $\approx 64\%$, Gradual $\approx 43\%$, Blip $\approx 0\%$. Tổng SUB micro-average $= 65.8\%$, macro-average per-class $= 55.7\%$.}
	\label{fig:confusion-5class}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{../../results/plots/confusion_matrix_category.png}
	\caption[Confusion Matrix --- Category Level]{Confusion Matrix ở mức nhóm (TCD vs PCD, oracle mode). Độ chính xác nhóm đạt $81.9\%$; drift chậm (Gradual, Incremental) tiếp tục chồng lấn tín hiệu nhiều hơn drift nhanh (Sudden, Blip).}
	\label{fig:confusion-category}
\end{figure}

\begin{itemize}
	\item \textbf{Sudden Drift ($82.4\%$):} Điểm mạnh ổn định. Tín hiệu MMD tạo đỉnh nhọn, đối xứng (Hình~\ref{fig:signal_sudden}), dễ tách qua ngưỡng độ cao và độ nổi bật của đỉnh. Bộ sinh dữ liệu luân phiên các tập đặc trưng độc lập giữa các concept, nên Sudden hiếm khi bị Concept Memory phân loại nhầm thành Recurrent.

	\item \textbf{Recurrent Drift ($89.0\%$):} Cao nhất trong 5 loại. Có hai thành phần chính tạo nên kết quả này: (i) mô-đun \textbf{Concept Memory} (Mục~\ref{sec:se-cdt-concept-memory}) cho phép cây quyết định ghi nhớ các khái niệm đã xuất hiện thay vì coi mỗi lần như một sự kiện Sudden độc lập; (ii) kịch bản dữ liệu recurrent được xây dựng theo chu kỳ A$\leftrightarrow$B rõ ràng, đúng với định nghĩa của Lu et al.~\cite{lu2018learning}.

	\item \textbf{Incremental Drift ($64.4\%$):} Nhánh phân loại Incremental dùng các đặc trưng theo thời gian (LTS, MS, SDS) trong cây quyết định: khi tín hiệu MMD không có đỉnh rõ nhưng xu hướng tăng tuyến tính đủ mạnh, mẫu vẫn được nhận đúng là Incremental. Phần còn lại bị nhầm lẫn với Gradual do hai loại có hình dạng tín hiệu tương đồng (Hình~\ref{fig:signal_incremental}).

	\item \textbf{Gradual Drift ($42.6\%$):} Hai nguyên nhân chính: (i) trong khoảng chuyển tiếp dài, tín hiệu có thể tạo nhiều đỉnh phụ và một số trường hợp bị nhầm thành Recurrent; (ii) lớp Incremental có thêm đặc trưng theo thời gian nên một số trường hợp Gradual có xu hướng tuyến tính nhẹ bị phân nhầm. Đây là hạn chế của luật phân loại dạng cây với ngưỡng cố định; hướng cải tiến là thay bằng mô hình xác suất như thảo luận ở Chương~\ref{chap:conclusion}.

	\item \textbf{Blip Drift ($0.0\%$):} Đây là điểm yếu lớn nhất hiện tại. Bộ phân loại Blip dùng các đặc trưng tương đối (PPR, DPAR, WR) để giữ tính độc lập với kích thước cửa sổ, không dùng ngưỡng khoảng cách peak tuyệt đối. Luật yêu cầu đúng hai peak cô lập với khoảng cách gần nhau, biên độ tương đương và độ rộng đỉnh nhỏ, nhưng tín hiệu Blip thực tế thường có nhiễu phụ làm xuất hiện ba đến bốn peak nên không thoả luật. Hướng cải tiến tiếp theo là nới luật để chấp nhận thêm trường hợp ba peak và đánh giá lại trên benchmark mở rộng.
\end{itemize}

\subsection{So sánh với State-of-the-Art}
\label{sec:classification-sota}

Bảng~\ref{tab:cdt-comparison-by-type} so sánh với CDT-MSW~\cite{guo2022cdtmsw}, một phương pháp supervised sử dụng label và dữ liệu UCI thực tế.

\begin{table}[H]
	\centering
	\caption{So sánh độ chính xác phân loại theo từng loại drift: CDT-MSW (Guo et al.~2022) vs SE-CDT. \textit{Lưu ý:} ACC\_subcat của CDT-MSW được tính có điều kiện (trên các drift phát hiện đúng và phân nhóm đúng).}
	\label{tab:cdt-comparison-by-type}
	\input{../../results/tables/table_cdt_msw_vs_se_cdt_by_drift_type.tex}
\end{table}

\textbf{Cảnh báo về tính tham khảo của bảng so sánh.} Bảng~\ref{tab:cdt-comparison-by-type} đặt cạnh nhau các con số được công bố trong paper gốc CDT-MSW~\cite{guo2022cdtmsw} (cột trái) và các con số đo trên benchmark synthetic của luận văn (cột phải). Đây \textit{không} phải là so sánh đối đầu (head-to-head) trên cùng một benchmark; hai cột vận hành trên hai phân phối dữ liệu, hai chế độ giám sát, và hai cách tính tổng hợp khác nhau:
\begin{itemize}
	\item \textbf{Dữ liệu khác nhau:} CDT-MSW chạy trên 10 tập UCI thực tế (Banana, Titanic, Thyroid, KDDCup99, MITface, USPS, \dots) với số lượng features và độ phức tạp khác nhau; SE-CDT chạy trên 6 kịch bản synthetic Gaussian 5 chiều đã mô tả ở Mục~\ref{sec:classification-setup}.
	\item \textbf{Mức giám sát khác nhau:} CDT-MSW là phương pháp \textit{supervised}, dùng nhãn $y$ để tính accuracy ratio; SE-CDT là \textit{unsupervised}, chỉ dựa trên $P(X)$.
	\item \textbf{Cách tính khác nhau:} ACC\_subcat của CDT-MSW là \textit{conditional} (tính trên các drift đã được phát hiện đúng và phân nhóm đúng); SUB của SE-CDT là \textit{unconditional} (tính trên toàn bộ sự kiện ground truth, kể cả những lần phát hiện sai). Với SE-CDT, macro-average theo lớp là $55.7\%$ như đã trình bày ở Bảng~\ref{tab:se-cdt-performance-by-type}.
	\item \textbf{Recall không cùng metric:} Trên paper gốc, CDT-MSW đạt MDR trung bình $17.5\%$, tương đương $\text{Recall} = 1 - \overline{\text{MDR}} = 82.5\%$\footnote{Trong paper~\cite{guo2022cdtmsw}, từ ``EDR'' được định nghĩa là \textit{Error Detection Rate} (tỉ lệ báo nhầm, càng thấp càng tốt), khác với cách đặt tên EDR (\textit{Event Detection Rate}, tương đương Recall) ở luận văn này. Vì vậy giá trị 82.5\% được suy từ MDR Bảng 5 của paper.}.
\end{itemize}
Vì các điều kiện vận hành khác nhau, bảng nên được đọc theo nghĩa: SE-CDT chạy không cần nhãn trên dữ liệu Gaussian 5-D có ground truth chính xác, và đạt được mức accuracy phân loại nằm trong phạm vi cạnh tranh với CDT-MSW supervised trên dữ liệu UCI thực tế. Luận văn không khẳng định SE-CDT vượt CDT-MSW trên cùng setup. Một so sánh đối đầu thực sự cần triển khai SE-CDT trên chính các tập UCI gốc và đo lại CDT-MSW trên dữ liệu synthetic của luận văn; đây là hướng phát triển tự nhiên (Chương~\ref{chap:conclusion}).

\section{Đánh giá khả năng thích ứng mô hình}
\label{sec:adaptation-eval}

Sau khi phát hiện drift, hệ thống kích hoạt quy trình thích ứng. Phần này đánh giá hiệu quả của chiến lược thích ứng theo loại drift (Type-Specific Adaptation) sử dụng chỉ số \textbf{Prequential Accuracy}.

\subsection{Thiết lập thực nghiệm}
\begin{itemize}
	\item \textbf{Môi trường:} Prequential Evaluation trên luồng dữ liệu mô phỏng.
	\item \textbf{Dữ liệu:} $T = 5{,}000$ mẫu, mật độ drift cao (5 sự kiện/luồng).
	\item \textbf{Kịch bản:} ba loại: Stepping (drift tích lũy), Sudden, và Mixed.
	\item \textbf{Bốn chiến lược thích ứng:}
	\begin{itemize}
		\item \textit{No Adaptation:} giữ nguyên mô hình ban đầu (cận dưới).
		\item \textit{Periodic Retrain:} huấn luyện lại định kỳ mỗi 500 mẫu, không dùng tín hiệu detector.
		\item \textit{Simple Retrain:} huấn luyện lại trên cửa sổ gần nhất mỗi khi SE-CDT báo drift, bất kể loại nào.
		\item \textit{Type-Specific (đề xuất):} chọn một trong bốn chiến lược (Full Reset, Incremental Fine-tune, Recency-weighted, Model Caching) tuỳ theo nhãn drift do SE-CDT phân loại (xem Mục~\ref{sec:model-adaptation-framework}).
	\end{itemize}
	\item \textbf{Tham số:} ngưỡng phát hiện $\tau = 0.015$, cửa sổ ShapeDD $W = 50$, mô hình nền là Logistic Regression với số lần lặp tối đa 1000.
	\item \textbf{Chỉ số:} Prequential Accuracy (độ chính xác cuốn chiếu) đo khả năng phục hồi sau drift; số False Positives (FP) đo chi phí cảnh báo dư thừa.
\end{itemize}

\paragraph{Bộ sinh dữ liệu.} Luồng được tạo từ hỗn hợp Gaussian năm chiều với năm điểm drift đều nhau. Theo định nghĩa của Lu và cộng sự~\cite{lu2018learning}, mỗi sự kiện Sudden, Gradual hoặc Incremental phải dẫn đến một concept mới, còn Recurrent là sự lặp lại có chu kỳ giữa hai concept. Để đáp ứng điều này, bộ sinh dùng cơ chế xoay vòng đặc trưng: mỗi sự kiện non-recurrent dịch chuyển một tập đặc trưng khác nhau, tránh quay về phân phối đã thấy. Riêng Recurrent giữ pattern luân phiên $A \leftrightarrow B$ đúng theo định nghĩa. Kịch bản Stepping bổ sung thêm thành phần dịch chuyển tích lũy theo cùng hướng để mô phỏng môi trường tiến hoá đơn điệu (ví dụ trôi cảm biến công nghiệp).

\subsection{Phân biệt Real Drift và Virtual Drift trong thích ứng}
Như đã thảo luận ở Mục~\ref{sec:overview} và Mục~\ref{sec:detection-eval}, các phương pháp unsupervised như SE-CDT được thiết kế để phát hiện Real Drift (thay đổi $P(X)$) và bỏ qua Virtual Drift (chỉ đổi $P(Y|X)$). Vì vậy, các kết quả thích ứng dưới đây tập trung vào kịch bản Real Drift, nơi tín hiệu của SE-CDT có thể được dùng để kích hoạt cập nhật mô hình.

\subsection{Kết quả phục hồi (Recovery)}

Các bộ tạo dữ liệu Sudden Drift truyền thống thường luân phiên giữa hai concept A $\to$ B $\to$ A, dẫn đến mô hình tĩnh có độ chính xác ảo $\approx 75\%$. Để khắc phục giới hạn này, luận văn bổ sung kịch bản \textbf{Stepping Drift}. Đây là dạng trôi dạt tích lũy (Cumulative Sudden Drift), nơi phân phối dữ liệu liên tục dịch chuyển ra xa trạng thái ban đầu ($P_0 \to P_1 \to P_2 \dots$), buộc mô hình phải học lại liên tục để duy trì hiệu suất.

Ngoài ra, hệ thống tích hợp cơ chế \textbf{reset bộ đệm theo mẫu ADWIN} (Adaptive Windowing): sau mỗi lần thích ứng, bộ đệm được xóa và chỉ giữ lại 200 mẫu gần nhất làm tham chiếu mới. Cơ chế này giảm đáng kể hiện tượng phát hiện lại cùng một drift event: Type-Specific và Simple Retrain (cả hai đều dùng tín hiệu detector để kích hoạt) có FP trung bình $\sim 2$/run, trong khi Periodic Retrain và No Adaptation (không thay đổi bộ đệm theo detector) có FP trung bình $\sim 16$/run.

Bảng~\ref{tab:prequential-results} tổng hợp kết quả Prequential Accuracy trên $N = 30$ lần chạy độc lập, so sánh 4 chiến lược: Type-Specific (đề xuất), Simple Retrain, Periodic Retrain và No Adaptation. Số $N = 30$ đủ để kiểm định Friedman có lực thống kê cao và đảm bảo khoảng tin cậy 95\% chặt với độ lệch chuẩn quan sát được trên các kịch bản.

\begin{table}[H]
	\centering
	\caption{Kết quả Prequential Accuracy theo loại drift ($N=30$ runs, $T=5000$ mẫu). Cột FP: số lần báo động sai trung bình mỗi run, theo thứ tự chiến lược (Type-Specific / Simple Retrain / Periodic Retrain / No Adaptation).}
	\label{tab:prequential-results}
	\resizebox{\textwidth}{!}{%
	\begin{tabular}{|l|c|c|c|c|c|}
		\hline
		\textbf{Loại Drift} & \textbf{Type-Specific} & \textbf{Simple Retrain} & \textbf{Periodic Retrain} & \textbf{No Adaptation} & \textbf{FP} \\
		\hline
		Stepping (Cumulative) & \textbf{71.37\%} $\pm 3.07$ & 62.95\% $\pm 2.60$ & 64.03\% $\pm 2.74$ & 54.80\% $\pm 3.60$ & $1.8 / 1.8 / 18.4 / 18.4$ \\
		Sudden (Joint Drift)  & \textbf{70.78\%} $\pm 2.85$ & 62.16\% $\pm 2.29$ & 63.65\% $\pm 2.37$ & 53.68\% $\pm 3.46$ & $1.9 / 1.9 / 18.6 / 18.6$ \\
		Mixed (Combined)      & \textbf{86.20\%} $\pm 1.21$ & 84.24\% $\pm 1.06$ & 86.09\% $\pm 0.69$ & 82.69\% $\pm 0.94$ & $2.7 / 2.7 / 15.5 / 15.5$ \\
		\hline
	\end{tabular}%
	}
\end{table}

\paragraph{Kiểm định Friedman.} Áp dụng kiểm định không tham số Friedman (paired theo seed) trên 4 chiến lược $\times$ 30 runs cho mỗi kịch bản:
\begin{itemize}
	\item Stepping: $\chi^2 = 78.12$, $p = 7.77 \times 10^{-17}$
	\item Sudden:   $\chi^2 = 86.76$, $p = 1.09 \times 10^{-18}$
	\item Mixed:    $\chi^2 = 76.32$, $p = 1.89 \times 10^{-16}$
\end{itemize}
Trên cả ba kịch bản, $p \ll 0.05$ nên có đủ bằng chứng thống kê để bác bỏ giả thuyết H0 ``bốn chiến lược cho cùng phân phối accuracy''. Khác biệt giữa các chiến lược do đó là có ý nghĩa thống kê chứ không phải dao động ngẫu nhiên.

\begin{itemize}
	\item \textbf{Stepping ($+16.57$pp so với No Adaptation):} Khi không thích ứng, độ chính xác giảm về $54.80\%$ do phân phối liên tục dịch chuyển. Type-Specific giữ ở $71.37\%$, hơn Simple Retrain $+8.42$pp và Periodic Retrain $+7.34$pp. Hai baseline retrain không phân biệt loại đạt kết quả gần như nhau (khoảng $63\%$), cho thấy việc lựa chọn chiến lược theo loại drift đem lại lợi ích lớn hơn việc chỉ chọn thời điểm retrain.
	\item \textbf{Sudden ($+17.10$pp so với No Adaptation):} Type-Specific đạt $70.78\%$, hơn Simple Retrain $+8.62$pp và Periodic Retrain $+7.13$pp. Mỗi sự kiện ở đây là concept mới, vì vậy chiến lược Full Reset trên cửa sổ post-drift hoạt động đúng nhịp: mô hình cũ bị thay thế ngay khi không còn phù hợp, tránh kéo theo dữ liệu pre-drift đã không còn giá trị.
	\item \textbf{Mixed ($+3.51$pp so với No Adaptation):} Type-Specific đạt $86.20\%$, hơn Simple Retrain $+1.96$pp và Periodic Retrain $+0.11$pp. Trên kịch bản này, Periodic Retrain bám sát Type-Specific vì lịch retrain định kỳ ($500$ mẫu) trùng nhịp với mật độ drift dày đặc; nhưng Periodic Retrain có FP cao gấp $\sim 6\times$ ($15.5$ vs $2.7$), thể hiện chi phí của việc retrain mù.
	\item \textbf{False Positives.} Type-Specific và Simple Retrain có FP thấp ($1.8$--$2.7$/run) nhờ cơ chế thu hẹp bộ đệm sau mỗi lần thích ứng, ngăn detector kích hoạt nhiều lần trên cùng một sự kiện. Periodic Retrain và No Adaptation không thay đổi bộ đệm theo tín hiệu detector nên ghi nhận nhiều cảnh báo lặp ($\sim 15$--$18$/run). Số FP $> 0$ ở đây phản ánh test Gamma-approximation đã được hiệu chỉnh đúng tại $\alpha = 0.05$ (Mục~\ref{sec:h0-calibration}).
	\item \textbf{Độ tin cậy thống kê.} Với $N = 30$ và độ lệch chuẩn $\sim 3\%$, khoảng tin cậy 95\% là $\pm 1.1\%$, đủ chặt để khẳng định chênh lệch $> 7\%$ giữa Type-Specific và các baseline là có ý nghĩa thống kê (Friedman $p \ll 0.05$).
\end{itemize}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{../../results/plots/fig_prequential_stepping.png}
	\caption[Prequential Accuracy --- Stepping Drift]{Prequential Accuracy trên Stepping Drift ($N = 30$). No Adaptation (đỏ) tụt đều về $\sim 55\%$ khi phân phối tiếp tục dịch chuyển. Type-Specific (xanh) giữ $\sim 71\%$ nhờ chọn đúng chiến lược thích ứng cho từng sự kiện. Simple và Periodic cùng đạt $\approx 63\%$ do không phân biệt loại drift.}
	\label{fig:prequential_stepping}
\end{figure}

\paragraph{So sánh với Sudden Drift.}
Trên Sudden Drift (Hình~\ref{fig:prequential_sudden}), No Adaptation giảm về $53.68\%$ do toàn bộ phân phối thay đổi sau mỗi sự kiện. Type-Specific đạt $70.78\%$, cách Simple Retrain $+8.62$pp và Periodic Retrain $+7.13$pp. Cả hai kịch bản Stepping và Sudden cho cùng mức kết quả (Type-Specific $\approx 71\%$, No Adaptation $\approx 54\%$, Simple/Periodic $\approx 63\%$) với cùng pipeline, chỉ ra rằng phần lớn cải thiện đến từ bước phân loại drift trước khi quyết định cách thích ứng, không phải từ riêng cơ chế phát hiện.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.95\textwidth]{../../results/plots/fig_prequential_sudden.png}
	\caption[Prequential Accuracy --- Sudden Drift]{Prequential Accuracy trên Sudden Drift ($N = 30$). No Adaptation giảm về $\sim 54\%$; Type-Specific duy trì $\sim 71\%$ nhờ chọn Full Reset cho từng sự kiện Sudden mới.}
	\label{fig:prequential_sudden}
\end{figure}

\section{Kiểm thử nguyên mẫu trên kiến trúc tương thích Kafka}
\label{sec:real-world-deployment}

Để kiểm tra luồng xử lý khép kín của kiến trúc đề xuất, luận văn xây dựng một nguyên mẫu trên Docker gồm Redpanda (nền tảng tương thích Kafka), SE-CDT Consumer và Adaptation Manager. Mục này nhằm kiểm tra khả năng phối hợp giữa các thành phần, không phải đánh giá hiệu năng hạ tầng ở quy mô sản xuất.

\subsection{Kịch bản triển khai}
\begin{itemize}
	\item \textbf{Dữ liệu:} Luồng dữ liệu mô phỏng từ SEA dataset với Sudden Drift tại mẫu thứ 1500.
	\item \textbf{Cấu hình:} Cửa sổ phát hiện $W = 250$ mẫu, độ trễ thích ứng $\Delta = 50$ mẫu (để thu thập đủ dữ liệu sau điểm drift trước khi cập nhật).
	\item \textbf{Mục tiêu:} Xác nhận quy trình khép kín: Phát hiện Drift $\rightarrow$ chờ thu thập dữ liệu mới $\rightarrow$ cập nhật mô hình $\rightarrow$ nạp lại mô hình $\rightarrow$ phục hồi độ chính xác.
\end{itemize}

\subsection{Kết quả thực tế}

Hình~\ref{fig:kafka_real_results} trình bày dashboard giám sát từ lần chạy nguyên mẫu:

\begin{figure}[H]
	\centering
	\includegraphics[width=1.0\textwidth]{image/kafka_results_real.png}
	\caption[Kết quả kiểm thử nguyên mẫu Kafka]{Dashboard giám sát nguyên mẫu tương thích Kafka. (1) Dữ liệu luồng với điểm drift tại mẫu 1500. (2) Độ chính xác giảm xuống 0.59 tại điểm drift, sau đó phục hồi lên 0.93 sau khi mô hình được cập nhật tự động. (3) Tín hiệu SE-CDT phát hiện Sudden Drift. (4) Tài nguyên hệ thống (CPU/RAM) duy trì ổn định trong lần chạy thử.}
	\label{fig:kafka_real_results}
\end{figure}

\begin{itemize}
	\item \textbf{Độ trễ phát hiện:} Drift được đặt tại mẫu 1500 trong dữ liệu. ShapeDD xác định điểm drift tại mẫu 1476. Đây là vị trí đỉnh tam giác ước lượng (triangle peak position) từ tín hiệu tích chập ShapeDD, không phải thời điểm xử lý thực. Do cơ chế cửa sổ trượt và lọc hình dạng, vị trí drift báo cáo có thể lệch so với nhãn danh nghĩa $\pm l_1$ (với $l_1 = 50$ là kích thước nửa cửa sổ reference), nằm trong giới hạn chấp nhận của ShapeDD.
	\item \textbf{Quy trình khép kín:} Log hệ thống xác nhận Consumer đã nhận tín hiệu cập nhật từ bộ thích ứng và tải lại mô hình thành công tại mẫu 2277.
	\item \textbf{Hiệu quả phục hồi:} Độ chính xác tăng từ mức 59\% (gần ngẫu nhiên) lên 93\% ngay sau khi mô hình mới được tải, khớp với hành vi mong đợi của pipeline.
\end{itemize}

\section{Kết luận chương}

Có thể tóm tắt các kết quả thực nghiệm như sau:
\begin{enumerate}
	\item \textbf{Detection:} ShapeDD-IDW (tín hiệu Standard MMD + kiểm định IDW-MMD bằng xấp xỉ Gamma) đạt $F_1 = 0.459$ với $\sim 14.5$ FP/run trên benchmark phát hiện ($\delta = 75$, 14 datasets, 30 seeds), nhỉnh hơn ShapeDD gốc ($F_1 = 0.433$, $18.4$ FP) với thời gian chạy tương đương (Bảng~\ref{tab:runtime_stats}). Kiểm định IDW-MMD bằng xấp xỉ Gamma đạt Type-I error $0.040$--$0.075$ trên ba phân phối tĩnh; khi ghép vào SE-CDT, Type-I error có thể tăng tới $0.100$ do kiểm định nhiều đỉnh ứng viên (Mục~\ref{sec:h0-calibration}).
	\item \textbf{Classification:} SE-CDT (Standard MMD + Concept Memory + Self-Calibration) đạt CAT $= \textbf{81.9\%}$, SUB micro-average $= \textbf{65.8\%}$, SUB macro-average $= \textbf{55.7\%}$ ở oracle mode mà không cần nhãn. Tỉ lệ phát hiện sự kiện EDR đạt \textbf{92.6\%}. Recurrent đạt $\textbf{89.0\%}$ nhờ Concept Memory; Incremental đạt $64.4\%$ nhờ bổ sung các đặc trưng theo thời gian vào cây quyết định.
	\item \textbf{Adaptation:} Trên kết quả $N = 30$ với kiểm định Friedman, Type-Specific Adaptation hơn No Adaptation $+16.57$pp trên Stepping ($71.37\%$ vs $54.80\%$), $+17.10$pp trên Sudden ($70.78\%$ vs $53.68\%$), và $+3.51$pp trên Mixed ($86.20\%$ vs $82.69\%$). Type-Specific cũng vượt Simple Retrain ($+1.96$ đến $+8.62$pp) và Periodic Retrain ($+0.11$ đến $+7.34$pp) trên cả ba kịch bản, với khác biệt có ý nghĩa thống kê mạnh ($p < 10^{-15}$). Cơ chế thu hẹp bộ đệm sau mỗi lần thích ứng giữ FP thấp ($\sim 2$/run) cho Type-Specific và Simple Retrain so với $\sim 16$/run của các baseline không dùng detector signal.
\end{enumerate}

Kết quả này cho thấy kiến trúc đề xuất khả thi cho các hệ thống giám sát luồng dữ liệu thời gian thực.

\textbf{Phạm vi benchmark và hướng mở rộng.} Benchmark detection tập trung chủ yếu vào sudden/abrupt drift và blip, đúng với trọng tâm thiết kế của ShapeDD gốc~\cite{shapeDD2024} là phát hiện thay đổi phân phối đột ngột. Các loại drift chậm hơn (gradual, incremental) và drift lặp lại (recurrent) được đưa vào benchmark phân loại như mở rộng năng lực, không phải trọng tâm tối ưu. Kết quả phân loại theo từng loại drift (Bảng~\ref{tab:se-cdt-performance-by-type}) phản ánh đúng độ khó vốn có và mở ra hướng cải tiến cho các công trình tiếp theo.
